---
title: Proxy Setup
description: Configure direct proxy settings for the agent worker.
---

# Proxy Setup

Proxy settings apply to `apps/agent`.

Set:

- `PROXY_HOST`
- `PROXY_PORT`
- `PROXY_USERNAME` (optional)
- `PROXY_PASSWORD` (optional; must be set with `PROXY_USERNAME`)

Credentials are passed with `PROXY_USERNAME`/`PROXY_PASSWORD`.

## Operational Guidance

- Keep proxy credentials out of source control.
- Track provider-specific failure rates.
- Increase `AGENT_WORKER_CONCURRENCY` only after proxy stability is verified.
